/* No inline CSS anywhere */

.menu-box {
  background-color: #e0e0e0;
  padding: 20px;
  margin-top: 20px;
  min-height: 1000px; /* Forces scrolling */
}

/* Center dropdown text */
.navbar-nav > li > a {
  text-align: center;
}
